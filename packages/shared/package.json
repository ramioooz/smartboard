{
  "name": "@smartboard/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared DTOs, Zod schemas, event contracts, and types for the smartboard platform",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@smartboard/config": "workspace:*",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
