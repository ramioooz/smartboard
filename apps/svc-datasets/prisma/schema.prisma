generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model — real schema migrations come in a later phase
model Dataset {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
